// @maipai/ui: empty until ui-v0.1.0 extracts the reconciled kit and
// shell from the Stack (docs/dev.md). This placeholder exists so the
// workspace's own lint and tests are real from the skeleton commit on.
export const UI_WORKSPACE_PLACEHOLDER = true;
