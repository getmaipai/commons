{
  "name": "@maipai/core",
  "version": "0.0.0",
  "private": true,
  "description": "MaiPai's product-agnostic helpers: logging, timeouts, paths, archives, identity, secrets, rate limiting, hardware, backup crypto. See ../docs/dev.md.",
  "license": "AGPL-3.0",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.4.1",
    "typescript": "^5.9.3"
  }
}
