# Changelog (`@maipai/core`)

All notable changes to the `core` workspace. Format follows
[Keep a Changelog](https://keepachangelog.com); versions follow semver,
tagged `core-vX.Y.Z`. Everything stays `0.x` until Home's and the Stack's
adoption proves it.

## [Unreleased]

### Added
- The workspace skeleton: `package.json`, `tsconfig.json`, a placeholder
  export and test. No helpers yet; lands at `core-v0.1.0`.
