// @maipai/core: empty until core-v0.1.0 extracts the shared helpers from
// Home and the Stack (docs/dev.md). This placeholder exists so the
// workspace's own lint and tests are real from the skeleton commit on.
export const CORE_WORKSPACE_PLACEHOLDER = true;
