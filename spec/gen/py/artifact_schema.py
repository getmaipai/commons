# generated by datamodel-codegen:
#   filename:  artifact.schema.json

from __future__ import annotations

from typing import Literal

from pydantic import AwareDatetime, BaseModel, ConfigDict, Field, conint, constr


class Artifact(BaseModel):
    """
    One immutable version of a generated document (markdown, code, or html) an assistant turn created or updated for the chat's artifact-card/canvas-split experience. Distinct from TurnArtifact (turn-artifact.schema.json), COMP-01's evidence-grounded details document built from retained tool outcomes - this is the model's own authored content, versioned like a file. A later version is a new row with a new id, chained by parent_version; an existing version is never edited in place. Deliberately not TurnArtifact's own revision scheme (a bare incrementing integer, no id-based chain): this record follows conversation-turn.schema.json's parent_turn_id convention instead, because the chat wire needs a version's own id on the stream (the artifact-card/canvas-split tool call and the message metadata both name a specific version by id, the way parent_turn_id names a specific turn) and because id-chaining, unlike a bare counter, has somewhere to point if branching from a non-latest version is ever needed. validate.ts's/validate.py's validateArtifact() enforces the pairing (version 1 has no parent_version, every later version has one, and neither may equal its own id).
    """

    model_config = ConfigDict(
        extra='forbid',
    )
    id: constr(pattern=r'^art-[a-z0-9]{6,}$') = Field(
        ...,
        description='Stable identity for this specific version. A later version of the same artifact has a new id; parent_version is how a client walks the chain back to version 1.',
    )
    conversation_id: constr(pattern=r'^conv-[a-z0-9]{6,}$') = Field(
        ...,
        description="The conversation this version belongs to. A conversation may hold more than one independent artifact; parent_version, not this field, is what identifies one artifact's own version chain.",
    )
    turn_id: constr(pattern=r'^turn-[a-z0-9]{6,}$') = Field(
        ...,
        description='The assistant turn that produced this version, whether it created the artifact or edited a prior version into this one.',
    )
    kind: Literal['markdown', 'code', 'html']
    title: constr(min_length=1)
    body: constr(min_length=1) = Field(
        ...,
        description='The full content of this version, never a patch. A caller updating an existing artifact resolves any patch against the current version into a full body before this record is written, so any one version is independently renderable without replaying history.',
    )
    version: conint(ge=1) = Field(
        ...,
        description="One-based sequence number within this artifact's version chain. Display-only; parent_version is the real pointer a client walks.",
    )
    parent_version: constr(pattern=r'^art-[a-z0-9]{6,}$') | None = Field(
        ...,
        description="The id of the version this one replaces, or null for an artifact's first version.",
    )
    created_by: constr(pattern=r'^person-[a-z0-9]{6,}$') = Field(
        ...,
        description='The household member present on the turn that produced this version.',
    )
    provenance: constr(min_length=1) = Field(
        ...,
        description='Free-text provenance for the build, such as the turn-engine tool call that produced it.',
    )
    created_at: AwareDatetime
    hlc: constr(pattern=r'^[0-9]+:[0-9]+:[a-z0-9]{6,}$') = Field(
        ..., description='Hybrid logical clock: wall_ms:counter:node (7.3).'
    )
