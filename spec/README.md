# spec/ (not moved yet)

`@maipai/spec` lands here at `spec-v0.1.0` (refocus-work-order-2026-09-20.md,
step 0c): the household's record shapes, schemas, settings declaration
format, UI schema, errors, safety vocabulary, and streaming and voice
shapes, moved whole from `home/spec` (including `pyproject.toml`, the
Python package, `gen/`, `schemas.resolved/`, fixtures and tests).

Until that move, `home/spec` remains the source of truth; see
[home/spec/README.md](https://github.com/getmaipai/home/blob/main/spec/README.md).
