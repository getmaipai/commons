{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://getmaipai.github.io/shared/spec/schemas/turn-stream-event.schema.json",
  "title": "TurnStreamEvent",
  "description": "A turn-stream wire event: one line of the NDJSON stream a /stream turn emits. `tool_call`, `tool_result` and `tool_error` are the tool shapes (TOOL-EVENTS-01, plus `tool_result.outcome.sites` from TOOL-EVENTS-02: the sites a search step actually read, at most five, the same list that becomes the reply's own sources); the remaining union members (`turn_meta`, `signal`, `delta`, `reasoning`, `spoken_cue`, `done`, `error`) are defined by the home backend and not this schema - this schema covers only the tool-event shapes the spec must name.",
  "oneOf": [
    {
      "type": "object",
      "properties": {
        "t": { "const": "tool_call" },
        "package_id": { "type": "string", "minLength": 1 },
        "args": { "type": "object" },
        "call_id": { "type": "string", "minLength": 1 },
        "label": { "type": "string", "minLength": 1 }
      },
      "required": ["t", "package_id", "args", "call_id"],
      "additionalProperties": false
    },
    {
      "type": "object",
      "properties": {
        "t": { "const": "tool_result" },
        "call_id": { "type": "string", "minLength": 1 },
        "package_id": { "type": "string", "minLength": 1 },
        "outcome": {
          "type": "object",
          "properties": {
            "text": { "type": "string" },
            "error_code": { "type": "string" },
            "sites": {
              "type": "array",
              "maxItems": 5,
              "items": {
                "type": "object",
                "properties": {
                  "host": { "type": "string", "minLength": 1 },
                  "url": { "type": "string", "format": "uri" }
                },
                "required": ["host", "url"],
                "additionalProperties": false
              }
            }
          },
          "additionalProperties": false
        }
      },
      "required": ["t", "call_id", "package_id", "outcome"],
      "additionalProperties": false
    },
    {
      "type": "object",
      "properties": {
        "t": { "const": "tool_error" },
        "call_id": { "type": "string", "minLength": 1 },
        "package_id": { "type": "string", "minLength": 1 },
        "error": { "type": "string", "minLength": 1 }
      },
      "required": ["t", "call_id", "package_id", "error"],
      "additionalProperties": false
    }
  ]
}
