MaiPai Shared
Third-Party Licenses and Attributions

This repo holds the org's shared libraries (ui, core, spec), extracted
from Home and the Stack. Attributions below are added as each
workspace's own dependencies land, per docs/PACKAGES.md in
getmaipai/.github.

## core

- Hono (MIT) - https://hono.dev (`openapi.ts`'s router; `secretThrottle.ts`'s
  connection-info lookup)
- @hono/zod-openapi (MIT) - https://github.com/honojs/middleware
  (`openapi.ts`'s route/schema scaffolding)
- Zod (MIT) - https://zod.dev (`openapi.ts`'s schemas)

## ui

- React, React DOM (MIT) - https://react.dev (peer dependencies)
- Radix UI: react-popover, and the consolidated `radix-ui` package (MIT) -
  https://www.radix-ui.com
- lucide-react (ISC) - https://lucide.dev
- clsx (MIT) - https://github.com/lukeed/clsx
- tailwind-merge (MIT) - https://github.com/dcastil/tailwind-merge
- class-variance-authority (Apache-2.0) - https://cva.style
- tw-animate-css (MIT) - https://github.com/Wombosvideo/tw-animate-css
- cmdk (MIT) - https://cmdk.paco.me (the command palette primitive
  behind `ui/command.tsx`)
- sonner (MIT) - https://sonner.emilkowal.ski (the kit's one toast
  primitive, `ui/sonner.tsx`)
- next-themes (MIT) - https://github.com/pacocoursey/next-themes
  (`AppearanceControl`'s light/dark/system switching)
- vaul (MIT) - https://vaul.emilkowal.ski (`ui/drawer.tsx`)
- @tanstack/react-query (MIT) - https://tanstack.com/query
  (the settings and UI-schema renderers' data layer)
- react-router-dom (MIT) - https://reactrouter.com (the shell's nav,
  `PhoneNav`, `NotificationPopover`'s history link)
- recharts (MIT) - https://recharts.org (`ui/chart.tsx`)
- @noriginmedia/norigin-spatial-navigation (MIT) -
  https://github.com/NoriginMedia/Norigin-Spatial-Navigation
  (`tvNav.ts`, the far/TV surface's arrow-key focus)
- usehooks-ts (MIT) - https://usehooks-ts.com (`useSurface.ts`'s media
  query hooks)
- Zod (MIT) - https://zod.dev (the UI-schema types)

### `ui/src/dashboard/` - vendored from shadcndashboard

shadcndashboard (MIT) - https://github.com/shadcndashboard/shadcndashboard,
commit `6f99c0b04b7169f9ef12dc99946bc4faaeb40b9b` - see
`ui/docs/dashboard-upstream.md` for what was vendored and stripped. Its
own dependencies added here:

- @base-ui/react (MIT) - https://base-ui.com (the template's primitives)
- @tanstack/react-table (MIT) - https://tanstack.com/table (`DataTable`)
- embla-carousel-react (MIT) - https://www.embla-carousel.com
  (`ui/carousel.tsx`)
- input-otp (MIT) - https://github.com/guilhermerodz/input-otp
  (`ui/input-otp.tsx`)
- lodash (MIT) - https://lodash.com (`sidebaritems.ts`'s `uniqueId`)
- motion (MIT) - https://motion.dev (the sidebar nav item's hover
  animation)
- react-day-picker (MIT) - https://daypicker.dev (`ui/calendar.tsx`)
- react-resizable-panels (MIT) - https://github.com/bvaughn/react-
  resizable-panels (`ui/resizable.tsx`)
- react-router (MIT) - https://reactrouter.com (the template's own import
  path; Home's shell keeps `react-router-dom`, same library and major)
- simplebar-react (MIT) - https://github.com/Grsmto/simplebar
  (the sidebar's scroll area)
- uuid (MIT) - https://github.com/uuidjs/uuid (`recent-orders.tsx`)

### `ui/src/elements/` - vendored from assistant-ui's Elements

assistant-ui (MIT) - https://github.com/assistant-ui/assistant-ui,
`apps/registry` at commit `039c3c32822632f2a564164f089f538926886124` -
see `ui/docs/dashboard-upstream.md` for the install command and the
CLI/registry path mismatch fixed by hand. Its own dependencies added
here:

- cn (MIT) - https://github.com/lukeed/cn (the Elements' class-name
  helper, used directly rather than a local `lib/utils.ts` copy)
- tw-shimmer (MIT) - https://github.com/assistant-ui/assistant-ui
  (the loading-shimmer utility a couple of Elements use)

`sources.aui.tsx` - assistant-ui (MIT) -
https://github.com/assistant-ui/assistant-ui,
`packages/ui/src/components/react/assistant-ui/elements/sources.aui.tsx`
at commit `0dd6d46b0`, fetched directly (not through the registry CLI);
only its import paths were rewritten to the kit's own.

When another component lands, its attribution goes here.
